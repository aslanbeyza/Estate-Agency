# Base URL of the backend API.
#
# Local development:  leave this as-is (points at `npm run start:dev`)
# Vercel production:  set this in the Vercel project dashboard to the
#                     Render backend URL, e.g.
#                     https://estate-agency-api.onrender.com
NUXT_PUBLIC_API_BASE=http://localhost:3001
